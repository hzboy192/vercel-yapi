{
  "version": 2,
  "name": "vercel-yapi",
  "builds": [
    {
      "src": "server/app.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["static/**", "config.json"]
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server/app.js"
    },
    {
      "src": "/(.*)",
      "dest": "server/app.js"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "build": {
    "env": {
      "NPM_CONFIG_LEGACY_BUNDLES": "true",
      "NPM_CONFIG_IGNORE_SCRIPTS": "true"
    }
  }
}
